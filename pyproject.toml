[project]
name = "obsidian-to-anki"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.3.0",
    "pytest>=9.0.3",
]
